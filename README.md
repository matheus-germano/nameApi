# nameApi
 A basic API developed for a job application that receive a name, if the name contains the letter L, then the API has to return the inverted name, else the API has to return the name in lower case.
